RSpec.describe TheMealDbApiClient do
  it "has a version number" do
    expect(TheMealDbApiClient::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
